if (window.innerWidth < 1000) {
  // ウィンドウ幅が1000px未満の場合
  const swiper = new Swiper(".mySwiper-mobile", {
    navigation: false,
    // その他の設定
  });
} else {
  // ウィンドウ幅が1000px以上の場合
  const swiper = new Swiper(".mySwiper-desktop", {
    navigation: true,
    // その他の設定
  });
}